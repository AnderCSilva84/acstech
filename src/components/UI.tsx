import type { CSSProperties,ReactNode } from 'react'; import { motion } from 'framer-motion'; import { ArrowRight } from 'lucide-react'; import { Link } from 'react-router-dom'; import type { Project } from '../types/project';
export function Reveal({children,className=''}:{children:ReactNode;className?:string}){return <motion.div className={className} initial={{opacity:0,y:18}} whileInView={{opacity:1,y:0}} viewport={{once:true,margin:'-70px'}} transition={{duration:.55}}>{children}</motion.div>}
export function SectionHead({tag,title,text}:{tag:string;title:string;text?:string}){return <div className="section-head"><span className="kicker">{tag}</span><h2>{title}</h2>{text&&<p>{text}</p>}</div>}
export function ProjectVisual({project}:{project:Project}){return <div className={`project-visual${project.coverImage?' has-cover':''}`} style={{'--tone':project.tone} as CSSProperties} aria-hidden="true">{project.coverImage?<img src={project.coverImage} alt=""/>:<><div className="visual-top"><i/><i/><i/></div><div className="visual-layout"><span/><div><b/><b/><b/><b/></div></div><em>{project.name.slice(0,2).toUpperCase()}</em></>}</div>}
export function ProjectCard({project}:{project:Project}){return <article className="project-card"><ProjectVisual project={project}/><div className="project-card-body"><div className="card-meta"><span>{project.eyebrow}</span><span className="status">{project.status}</span></div><h3>{project.name}</h3><p>{project.summary}</p><div className="tags">{project.technologies.slice(0,4).map(t=><span key={t}>{t}</span>)}</div><Link className="text-link" to={`/projetos/${project.slug}`}>Conhecer projeto <ArrowRight size={16}/></Link></div></article>}
export function CTA(){return <Reveal><section className="cta"><span className="kicker">Seu próximo sistema</span><h2>Tem um processo que pode funcionar melhor?</h2><p>Conte o cenário. A ACS organiza o problema, desenha o caminho e constrói a solução.</p><Link className="button" to="/contato">Conversar sobre um projeto <ArrowRight size={17}/></Link></section></Reveal>}
