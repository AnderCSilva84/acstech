import { render,screen } from '@testing-library/react'; import userEvent from '@testing-library/user-event'; import { MemoryRouter } from 'react-router-dom'; import { describe,expect,it } from 'vitest'; import Projects from './Projects';
describe('catálogo',()=>{it('filtra projetos pela busca',async()=>{render(<MemoryRouter><Projects/></MemoryRouter>);await userEvent.type(screen.getByPlaceholderText(/buscar por projeto/i),'Benji');expect(screen.getByRole('heading',{name:'Benji'})).toBeInTheDocument();expect(screen.queryByRole('heading',{name:'ARTRAN'})).not.toBeInTheDocument()})});
